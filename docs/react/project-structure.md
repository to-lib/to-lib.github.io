---
sidebar_position: 27
title: 项目结构
---

# React 项目结构最佳实践

> [!TIP]
> 良好的项目结构能提升代码可维护性。本文提供多种组织方案。

## 📁 推荐结构

### 功能模块化（推荐）

```
src/
├── features/              # 功能模块
│   ├── auth/
│   │   ├── components/
│   │   │   ├── LoginForm.tsx
│   │   │   └── SignupForm.tsx
│   │   ├── hooks/
│   │   │   └── useAuth.ts
│   │   ├── api.ts
│   │   ├── store.ts
│   │   ├── types.ts
│   │   └── index.ts
│   ├── products/
│   └── cart/
├── components/            # 共享组件
│   ├── Button/
│   │   ├── Button.tsx
│   │   ├── Button.test.tsx
│   │   ├── Button.module.css
│   │   └── index.ts
│   ├── Modal/
│   └── Layout/
├── hooks/                 # 共享 Hooks
│   ├── useDebounce.ts
│   └── useLocalStorage.ts
├── utils/                 # 工具函数
│   ├── format.ts
│   └── validate.ts
├── types/                 # TypeScript 类型
│   └── global.d.ts
├── styles/                # 全局样式
│   ├── globals.css
│   └── variables.css
├── constants/             # 常量定义
│   └── config.ts
└── App.tsx
```

### 传统结构

```
src/
├── components/            # 所有组件
│   ├── common/           # 通用组件
│   └── specific/         # 特定组件
├── pages/                # 页面组件
├── services/             # API 服务
├── hooks/                # 自定义 Hooks
├── utils/                # 工具函数
├── types/                # 类型定义
├── assets/               # 静态资源
└── App.tsx
```

## 🚀 Next.js App Router 结构

```
├── app/                     # App Router 目录
│   ├── layout.tsx          # 根布局
│   ├── page.tsx            # 首页
│   ├── globals.css         # 全局样式
│   ├── error.tsx           # 错误页面
│   ├── loading.tsx         # 加载页面
│   ├── not-found.tsx       # 404 页面
│   │
│   ├── (auth)/             # 路由组（不影响 URL）
│   │   ├── login/
│   │   │   └── page.tsx    # /login
│   │   └── signup/
│   │       └── page.tsx    # /signup
│   │
│   ├── dashboard/
│   │   ├── layout.tsx      # 嵌套布局
│   │   ├── page.tsx        # /dashboard
│   │   ├── settings/
│   │   │   └── page.tsx    # /dashboard/settings
│   │   └── [id]/
│   │       └── page.tsx    # /dashboard/:id
│   │
│   └── api/                # API 路由
│       └── users/
│           └── route.ts    # /api/users
│
├── components/             # 共享组件
│   ├── ui/                # UI 组件
│   └── forms/             # 表单组件
│
├── lib/                   # 库和工具
│   ├── db.ts
│   └── auth.ts
│
└── public/                # 静态文件
```

## 📦 Monorepo 结构 (Turborepo)

```
my-monorepo/
├── apps/
│   ├── web/               # Next.js 应用
│   │   ├── app/
│   │   ├── package.json
│   │   └── tsconfig.json
│   ├── admin/             # 管理后台
│   │   └── ...
│   └── mobile/            # React Native
│       └── ...
│
├── packages/
│   ├── ui/                # 共享 UI 组件库
│   │   ├── src/
│   │   │   ├── Button.tsx
│   │   │   └── index.ts
│   │   └── package.json
│   ├── config-eslint/     # ESLint 配置
│   ├── config-typescript/ # TypeScript 配置
│   └── utils/             # 共享工具函数
│
├── turbo.json            # Turborepo 配置
├── package.json          # 根 package.json
└── pnpm-workspace.yaml   # pnpm workspace 配置
```

## 📝 文件命名规范

### 组件文件

```bash
# ✓ 推荐：PascalCase
Button.tsx
UserProfile.tsx
ShoppingCart.tsx

# ✓ 组件目录
Button/
├── Button.tsx
├── Button.test.tsx
├── Button.module.css
└── index.ts
```

### 非组件文件

```bash
# ✓ 推荐：camelCase
useAuth.ts           # Hooks
formatDate.ts        # 工具函数
authService.ts       # 服务

# ✓ 推荐：kebab-case (可选)
use-auth.ts
format-date.ts
```

### 测试文件

```bash
# 与源文件同名 + .test
Button.test.tsx
useAuth.test.ts

# 或放在 __tests__ 目录
__tests__/
├── Button.test.tsx
└── useAuth.test.ts
```

## 🎯 模块化组织原则

### 1. 功能内聚

```tsx
// features/auth/index.ts
// 统一导出该功能模块的所有公共 API
export { LoginForm } from "./components/LoginForm";
export { SignupForm } from "./components/SignupForm";
export { useAuth } from "./hooks/useAuth";
export type { User, AuthState } from "./types";
```

### 2. 依赖关系

```
┌─────────────────────────────────────────┐
│                  app                     │
├─────────────────────────────────────────┤
│              features/                   │
│   ┌─────────┬─────────┬─────────┐       │
│   │  auth   │ products│  cart   │       │
│   └────┬────┴────┬────┴────┬────┘       │
│        │         │         │             │
├────────┼─────────┼─────────┼────────────┤
│        ▼         ▼         ▼             │
│   ┌─────────────────────────────────┐   │
│   │     components / hooks / utils   │   │
│   │         (共享代码层)              │   │
│   └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

### 3. 导入路径别名

```json
// tsconfig.json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"],
      "@components/*": ["src/components/*"],
      "@features/*": ["src/features/*"],
      "@hooks/*": ["src/hooks/*"],
      "@utils/*": ["src/utils/*"]
    }
  }
}
```

```tsx
// 使用别名导入
import { Button } from "@components/Button";
import { useAuth } from "@features/auth";
import { formatDate } from "@utils/format";
```

## 💡 组织原则总结

| 原则           | 说明                             |
| -------------- | -------------------------------- |
| **按功能分组** | 相关文件放在一起，而非按类型分组 |
| **共享提取**   | 真正共享的代码才提升到顶层       |
| **浅层目录**   | 避免过深的嵌套（最多 3-4 层）    |
| **索引导出**   | 使用 `index.ts` 统一导出         |
| **命名一致**   | 团队统一命名规范                 |
| **就近原则**   | 测试文件与源文件放一起           |

---

**了解更多**：查看 [最佳实践](/docs/react/best-practices) | [TypeScript 配置](/docs/react/typescript)
