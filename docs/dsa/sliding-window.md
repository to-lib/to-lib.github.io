---
sidebar_position: 21
title: 滑动窗口
---

# 滑动窗口

滑动窗口（Sliding Window）是处理“**连续子数组/子串**”类问题的经典技巧。核心思想是维护一段区间 `[left, right]`，并在移动边界时增量维护窗口内信息（和、计数、频次等）。

## 📌 什么时候用滑动窗口

- 需要在数组/字符串里找：
  - **最长/最短**满足条件的连续区间
  - **固定长度**区间的最值/统计
  - 满足某个限制的“**连续**”子串/子数组

## 🧠 两种常见窗口

### 1) 固定长度窗口

每次右边扩张一个元素，同时左边收缩一个元素。

```java
// 长度为 k 的子数组最大和
public int maxSumFixedWindow(int[] nums, int k) {
    int windowSum = 0;
    int max = Integer.MIN_VALUE;

    for (int i = 0; i < nums.length; i++) {
        windowSum += nums[i];

        if (i >= k - 1) {
            max = Math.max(max, windowSum);
            windowSum -= nums[i - k + 1];
        }
    }

    return max;
}
```

### 2) 可变长度窗口（满足条件就收缩）

典型套路：
- 右指针扩张，更新窗口状态
- 当窗口“不满足”条件时，左指针收缩，直到重新满足
- 在过程中更新答案

```java
// 最小长度子数组，使得子数组和 >= target
public int minSubArrayLen(int target, int[] nums) {
    int left = 0;
    int sum = 0;
    int ans = Integer.MAX_VALUE;

    for (int right = 0; right < nums.length; right++) {
        sum += nums[right];

        while (sum >= target) {
            ans = Math.min(ans, right - left + 1);
            sum -= nums[left++];
        }
    }

    return ans == Integer.MAX_VALUE ? 0 : ans;
}
```

## ✅ 模板：最长无重复子串

这是字符串滑动窗口中最经典的模板之一，窗口内维护字符出现次数。

```java
public int lengthOfLongestSubstring(String s) {
    int[] cnt = new int[128];
    int left = 0;
    int ans = 0;

    for (int right = 0; right < s.length(); right++) {
        char c = s.charAt(right);
        cnt[c]++;

        while (cnt[c] > 1) {
            cnt[s.charAt(left)]--;
            left++;
        }

        ans = Math.max(ans, right - left + 1);
    }

    return ans;
}
```

## 🎯 复杂度

- **时间复杂度**：通常 O(n)
  - `left` 和 `right` 总共各移动最多 n 次
- **空间复杂度**：取决于窗口状态结构（如 `int[128]`、`HashMap`）

## 💡 常见坑

- **窗口条件写反**：到底是“满足就收缩”还是“不满足就收缩”。
- **更新答案的位置**：有时要在收缩前更新，有时要在收缩后更新。
